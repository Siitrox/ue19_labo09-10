name: Build and Publish Container

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to GitHub Packages
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push Docker image
      run: |
        CONTAINER_NAME=$(docker inspect --format='{{index .Config.Labels "name"}}' .)
        docker buildx build --platform linux/amd64,linux/arm64 -t ghcr.io/${{ github.repository }}/Docker_dad_jokes:latest .
        docker push ghcr.io/${{ github.repository }}/Docker_dad_jokes:latest
